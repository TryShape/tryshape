import React from "react";

import { CreateShape } from "../components";

const New = (props) => {
    return(
        <>
            <CreateShape {...props} />
        </>
    );
}

export default New;